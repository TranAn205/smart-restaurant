# ============================================
# SMART RESTAURANT - Docker Environment
# ============================================
# Copy file này thành .env và điền thông tin của bạn
# Windows: copy .env.docker.example .env
#
# QUAN TRỌNG: Bạn cần thay YOUR_IP bằng IP thực của máy
# Tìm IP: chạy "ipconfig | Select-String IPv4"
# Chọn IP dạng 192.168.x.x (cùng mạng WiFi với điện thoại)

# ============ DATABASE ============
DB_PASSWORD=postgres123

# ============ JWT ============
JWT_SECRET=smart-restaurant-jwt-secret-key-2024
QR_JWT_SECRET=qr_secret_key

# ============ QR CODE (Backend) ============
# URL Frontend - để backend tạo QR code đúng
# ⚠️ Thay YOUR_IP bằng IP thực của máy
CLIENT_BASE_URL=http://YOUR_IP.nip.io:3000

# ============ FRONTEND API ============
# URL Backend API - để frontend gọi API
# ⚠️ Thay YOUR_IP bằng IP thực của máy
NEXT_PUBLIC_API_URL=http://YOUR_IP:4000/api

# ============ GOOGLE OAUTH (Optional) ============
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============ EMAIL (Optional) ============
# Dùng Gmail App Password: https://myaccount.google.com/apppasswords
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
